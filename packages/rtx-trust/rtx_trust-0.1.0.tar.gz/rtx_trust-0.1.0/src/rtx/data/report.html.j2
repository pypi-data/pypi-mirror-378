<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Real Tracker X Report</title>
  <style>
    body { font-family: "Inter", system-ui, sans-serif; margin: 2rem; background: #0f172a; color: #e2e8f0; }
    h1 { color: #38bdf8; }
    .summary { display: flex; gap: 2rem; margin-bottom: 2rem; }
    .card { background: #1e293b; padding: 1rem 1.5rem; border-radius: 0.75rem; box-shadow: 0 10px 30px rgba(15, 23, 42, 0.4); }
    table { width: 100%; border-collapse: collapse; margin-top: 1rem; }
    th, td { padding: 0.75rem 1rem; border-bottom: 1px solid rgba(148, 163, 184, 0.2); }
    th { text-align: left; background: rgba(59, 130, 246, 0.1); }
    .severity-critical { color: #f87171; font-weight: bold; }
    .severity-high { color: #fb7185; font-weight: bold; }
    .severity-medium { color: #fbbf24; font-weight: bold; }
    .severity-low { color: #34d399; font-weight: bold; }
    .severity-none { color: #94a3b8; }
    .badge { display: inline-block; padding: 0.25rem 0.6rem; border-radius: 999px; font-size: 0.75rem; background: rgba(59, 130, 246, 0.2); }
    footer { margin-top: 3rem; font-size: 0.85rem; color: #94a3b8; }
  </style>
</head>
<body>
  <h1>Real Tracker X — Trust Report</h1>
  <p>Generated at {{ summary.generated_at }} for managers: {{ summary.managers | join(', ') }}</p>

  <div class="summary">
    {% for level, count in summary.counts.items() %}
    <div class="card">
      <div class="badge severity-{{ level }}">{{ level | upper }}</div>
      <h2>{{ count }}</h2>
      <p>Findings</p>
    </div>
    {% endfor %}
  </div>

  <table>
    <thead>
      <tr>
        <th>Dependency</th>
        <th>Verdict</th>
        <th>Score</th>
        <th>Advisories</th>
        <th>Signals</th>
      </tr>
    </thead>
    <tbody>
      {% for finding in findings %}
      <tr>
        <td>
          <strong>{{ finding.dependency }}</strong><br />
          <small>{{ finding.metadata.summary or '' }}</small>
        </td>
        <td class="severity-{{ finding.verdict }}">{{ finding.verdict | upper }}</td>
        <td>{{ '%.2f' | format(finding.score) }}</td>
        <td>
          {% if finding.advisories %}
            <ul>
              {% for advisory in finding.advisories %}
                <li><span class="badge severity-{{ advisory.severity }}">{{ advisory.severity | upper }}</span> {{ advisory.id }} — {{ advisory.summary }}</li>
              {% endfor %}
            </ul>
          {% else %}
            <em>None</em>
          {% endif %}
        </td>
        <td>
          {% if finding.signals %}
            <ul>
              {% for signal in finding.signals %}
                <li><span class="badge severity-{{ signal.severity }}">{{ signal.category }}</span> {{ signal.message }}</li>
              {% endfor %}
            </ul>
          {% else %}
            <em>None</em>
          {% endif %}
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <footer>
    Generated by Real Tracker X (Author: Andreas Fahl). SBOM and JSON versions accompany this report.
  </footer>
</body>
</html>
