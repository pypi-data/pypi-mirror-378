[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "HadeelAI"
version = "0.1.0"
description = "A simple search problem package"
authors = [
    { name = "Hadeel Dhiany", email = "hadeldhaiany2005@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

[tool.setuptools]
packages = ["HadeelAI"]