[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bauhaus-design-kit"
version = "0.1.0"
description = "Kit de herramientas de diseño inspirado en la Bauhaus"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/Marymvc/PG2_PRACT7.git"