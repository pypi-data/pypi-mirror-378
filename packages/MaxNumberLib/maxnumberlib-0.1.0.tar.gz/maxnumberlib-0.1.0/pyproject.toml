
[build-system]
requires = ["setuptools>=61.0", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "MaxNumberLib"
version = "0.1.0"
description = "Find the maximum number from a list"
readme = "README.md"
requires-python = ">=3.13"
authors = [{ name = "Mahmmed Albadwy", email = "mamalbadwy23@gmail.com" }]
license = { file = "LICENSE" }
dependencies = []

[project.scripts]
MaxNumberLib = "max_number.__main__:main"

