"""
Guest reports drops when roaming between floors; explain network behavior and quick fixes.
No tools; include small talk and an ATTENTION case beyond scope.

ROLES: user (guest walking between floors while on calls), assistant (connectivity guide explaining roaming expectations)
CHANNELS: analysis, commentary, final. Channel must be included for every message.
TOOLS: null
"""

system:
Hotel: Riverlight City Hotel — West Tower (hotel_id: rlh_west_02)
SSID (guest): Riverlight_Guest_W
Coverage: guestrooms, lobby, fitness (L2), terrace (L5)
Bandwidth cap: 100 Mbps/device; 4 devices per room
Support: 07:00–23:00 daily, ext. 2233
Notes:
- Captive portal appears once per device every 24 h
- Device roaming between floors may briefly re‑authenticate
- Tip: disable private MAC randomization to avoid captive loop
- Best for calls: pause at a landing to re‑associate, then continue

Quick Tips (bullets):
- Toggle Wi‑Fi off/on after floor change
- Use 5 GHz band if selectable
- Avoid elevator mid‑call

user:
My call drops when I go from 10 to 9. Anything I can do?

assistant channel=analysis:
Provide roaming behavior and quick tips from system.

assistant channel=final:
Roaming between floors can trigger a brief re‑authentication. Try toggling Wi‑Fi off/on after the floor change, use the 5 GHz band if available, and avoid the elevator mid‑call—pausing at the landing helps the phone re‑associate cleanly.

user:
Got it. What’s the device limit per room?

assistant channel=analysis:
Answer from system cap.

assistant channel=final:
You can connect up to 4 devices per room, each capped at 100 Mbps.

user:
Can you reduce the elevator music volume?

assistant channel=analysis:
Out of scope; use ATTENTION.

assistant channel=final:
[ATTENTION] Your note about the elevator music is received. Reviewing available options.

