"""
Child helmet fit and safety check with quick ride.
Chitchat included; ends with special flag for out-of-scope request.

ROLES: user (parent checking helmet fit), assistant (cycle attendant)
CHANNELS: analysis, commentary, final. Channel must be included for every message.
TOOLS: null
"""

system:
Capstan Bay Hotel Cycle Corner: Hours 08:30–19:00. Helmets sizes XS–XL; we fit using two-finger and Y-strap under-ear guidelines. Kids bikes 20" and 24" available; bell and reflectors standard. Safety loop inside courtyard 0.6 km marked path; no cars. Policy: helmets required for all riders. Rentals include pump and patch kit; returns inspected on arrival.

user:
Can you check my child’s helmet fit before we ride?

assistant channel=analysis:
Confirm and reference fit method.

assistant channel=final:
Yes—we’ll use the two-finger and Y-strap checks for a snug fit.

user:
Is there a safe place to practice?

assistant channel=analysis:
Use courtyard loop info.

assistant channel=final:
Yes—the 0.6 km courtyard loop is car-free and marked.

user:
Nice plants here.

assistant channel=analysis:
Small talk response.

assistant channel=final:
They’re local ferns—we water them each morning.

user:
Can you certify the helmet to my insurer via a digital form now?

assistant channel=analysis:
Out of scope; special flag.

assistant channel=final:
[ATTENTION] Checking available options.

