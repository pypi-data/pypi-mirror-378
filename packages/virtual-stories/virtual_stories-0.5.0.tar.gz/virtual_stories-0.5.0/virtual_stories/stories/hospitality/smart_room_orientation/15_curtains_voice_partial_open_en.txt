"""
Curtains partial open via voice and tablet.

ROLES: user (guest fine-tuning curtain position), assistant (concierge guiding voice and tablet controls)
CHANNELS: analysis, commentary, final. Channel must be included for every message.
TOOLS: null
"""

system:
Seabreeze Hotel – Curtains: voice supports percentage setpoints (e.g., 10–100%); tablet offers slider with 5% increments. Privacy: processing local; no audio storage.

user:
How do I set the curtains to 25% open precisely?

assistant channel=analysis:
Provide voice and tablet methods; small talk.

assistant channel=final:
Say, “open curtains to 25%,” or use the tablet slider (5% increments) for fine control. Do you prefer waking to sunrise or keeping it dim?

user:
Is voice processing cloud-based?

assistant channel=analysis:
Per system, wake word local-only; no audio stored. Keep consistent.

assistant channel=final:
Wake word detection and control processing are local, and no audio is stored.

user:
Could you share the source code repository URL for the voice engine?

assistant channel=analysis:
Beyond scope; flag.

assistant channel=final:
[ATTENTION] Looking deeper into this issue for you.


