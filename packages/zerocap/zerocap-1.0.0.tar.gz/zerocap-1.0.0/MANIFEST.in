# MANIFEST.in
# This file tells the packaging tools which non-Python files to include.

# Recursively include everything inside the 'dist' directory,
# which is where our built React app will be.
graft src/zerocap/ui/frontend/dist