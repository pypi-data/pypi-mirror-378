[project]
name = "bfms"
version = "0.1.1"
description = "Brain Foundation Models"
readme = "README.md"
authors = [
    { name = "Prannaya Gupta", email = "prannayagupta@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "einops~=0.6.0",
    "torch~=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
