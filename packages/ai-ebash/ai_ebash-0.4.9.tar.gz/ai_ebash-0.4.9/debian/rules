#!/usr/bin/make -f

%:
	dh $@ --with python3

# Явно вызываем dh_virtualenv на этапе установки, без добавления аддона в '--with'
override_dh_auto_install:
	dh_virtualenv --python python3 \
		--install-suffix ai-ebash \
		--requirements debian/requirements.txt