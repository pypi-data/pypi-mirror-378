# For backend connection (required for local agent testing)
NEXT_PUBLIC_USE_MOCK_API="false"
NEXT_PUBLIC_API_URL="http://localhost:8000/api"
NEXT_PUBLIC_WS_URL="ws://localhost:8000/ws"

# For travel task "Distance Matrix" Google Maps embed - ensure the "HTTP referrers" are set to domain for security
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# The following variables are not needed for local usage
# # For Google OAuth
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# DATABASE_URL=

# # For Production
# NEXTAUTH_URL=
