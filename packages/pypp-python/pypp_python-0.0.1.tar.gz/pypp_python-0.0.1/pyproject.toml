[project]
name = "pypp-python"
version = "0.0.1"
description = "contains necessary python code for Py++ projects"
authors = [{ name = "Curtis Puetz", email = "pypp.main@yahoo.com" }]
readme = "readme.md"
license = { text = "MIT" }
requires-python = ">=3.13"
urls = { "Homepage" = "https://github.com/curtispuetz/pypp-python" }


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"