[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "stack_spy"
version = "1.0.0"
description = "Stack analysis and discovery tool for webs."
authors = [
  { name="EssEnemiGz", email="biscenfabian@gmail.com" }
]
dependencies = ["aiohttp"]
