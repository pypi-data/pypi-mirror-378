[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "coco-core"
version = "0.0.2"
description = "A middleware rich backend framework for building APIs."
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "A S Nandanunni", email = "asnqln@gmail.com" }
]
