[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "localstack-dependencies"
version = "1.9"
description = 'dependencies for endpointer localstack-node'
readme = "README.md"
authors = [{name = 'endpointer'}]
license = {text = 'Apache License 2.0'}
dependencies = []

[project.urls]
Homepage = "https://github.com/endpointer-localstack/localstack-dependencies"

[tool.setuptools.packages.find]
where = ["."]
