[project]
name = "hello-poetry-e9999qian"
version = "0.1.0"
description = ""
authors = [
    {name = "eqian",email = "eqian@cisco.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "hello_poetry", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
