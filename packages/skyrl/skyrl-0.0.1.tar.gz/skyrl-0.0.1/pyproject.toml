[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "skyrl"
version = "0.0.1"
description = "Reserved initial release for the SkyRL project."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{ name = "Your Name", email = "you@example.com" }]
urls = { "Homepage" = "https://github.com/NovaSky-AI/SkyRL" }
