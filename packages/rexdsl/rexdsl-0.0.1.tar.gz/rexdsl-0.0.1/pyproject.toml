[project]
name = "rexdsl"
version = "0.0.1"
description = "custom regex dsl designed in rply for simplicity"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "regex>=2025.9.18",
    "rply>=0.7.8",
]

[tool.setuptools.packages.find]
include = ["rex*"]
