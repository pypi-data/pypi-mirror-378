[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-ala-library"
version = "0.1.0"
description = "My first Python library"
authors = [
  { name = "Alaa", email = "alaa123@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
dependencies = []
