[project]
name = "talvez"
version = "0.0.9"
description = "A lightweight Python implementation of a Maybe monad"
readme = "README.md"
keywords = ["maybe", "functional programming"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]
requires-python = ">=3.8"
license = { text = "GPL-3.0-or-later" }
authors = [
  { name = "Bruno Rodrigues" }
]
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project.urls]
Homepage = "https://github.com/b-rodrigues/talvez"
"Bug Tracker" = "https://github.com/b-rodrigues/talvez/issues"

[project.optional-dependencies]
dev = [
  "pytest>=7.0",
  "numpy"
]

[tool.setuptools.packages.find]
where = [
  "src"
]

[tool.pytest.ini_options]
pythonpath = [
  "src"
]
