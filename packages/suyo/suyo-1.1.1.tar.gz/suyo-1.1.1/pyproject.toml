[build-system]
requires = ["setuptools>=61.0","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "suyo"
version = "1.1.1"
description = "Suyo - essential helper package for HamzaGSopp projects"
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
authors = [ {name = "HamzaGSopp", email = "hs609187@gmail.com"} ]
dependencies = ["fs>=2.4"]

[tool.setuptools.packages.find]
where = ["src"]

# no console scripts; module provides `suyo.init()` and helpers
