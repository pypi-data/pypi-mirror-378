[build-system]
requires = ["setuptools>=61.0","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "suyo"
version = "1.0.0"
description = "Suyo — petit package créé et développé par HamzaGSopp"
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
authors = [ {name = "HamzaGSopp", email = "hs609187@gmail.com"} ]
dependencies = ["fs>=2.4"]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
"suyo-greet" = "suyo.cli:main"
