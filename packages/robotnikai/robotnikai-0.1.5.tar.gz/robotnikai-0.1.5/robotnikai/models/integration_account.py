# coding: utf-8

"""
    CUA API

    Documentation of API endpoints of CUA

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from robotnikai.models.social_account import SocialAccount
from robotnikai.models.user_integration_basic_auth import UserIntegrationBasicAuth
from robotnikai.models.user_integration_key import UserIntegrationKey
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

INTEGRATIONACCOUNT_ONE_OF_SCHEMAS = ["SocialAccount", "UserIntegrationBasicAuth", "UserIntegrationKey"]

class IntegrationAccount(BaseModel):
    """
    Account information. Type depends on integration type: SocialAccount for oauth, UserIntegrationBasicAuth for basic_auth, UserIntegrationKey for api_key
    """
    # data type: SocialAccount
    oneof_schema_1_validator: Optional[SocialAccount] = None
    # data type: UserIntegrationBasicAuth
    oneof_schema_2_validator: Optional[UserIntegrationBasicAuth] = None
    # data type: UserIntegrationKey
    oneof_schema_3_validator: Optional[UserIntegrationKey] = None
    actual_instance: Optional[Union[SocialAccount, UserIntegrationBasicAuth, UserIntegrationKey]] = None
    one_of_schemas: Set[str] = { "SocialAccount", "UserIntegrationBasicAuth", "UserIntegrationKey" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        if v is None:
            return v

        instance = IntegrationAccount.model_construct()
        error_messages = []
        match = 0
        # validate data type: SocialAccount
        if not isinstance(v, SocialAccount):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SocialAccount`")
        else:
            match += 1
        # validate data type: UserIntegrationBasicAuth
        if not isinstance(v, UserIntegrationBasicAuth):
            error_messages.append(f"Error! Input type `{type(v)}` is not `UserIntegrationBasicAuth`")
        else:
            match += 1
        # validate data type: UserIntegrationKey
        if not isinstance(v, UserIntegrationKey):
            error_messages.append(f"Error! Input type `{type(v)}` is not `UserIntegrationKey`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in IntegrationAccount with oneOf schemas: SocialAccount, UserIntegrationBasicAuth, UserIntegrationKey. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in IntegrationAccount with oneOf schemas: SocialAccount, UserIntegrationBasicAuth, UserIntegrationKey. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: Optional[str]) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        if json_str is None:
            return instance

        error_messages = []
        match = 0

        # deserialize data into SocialAccount
        try:
            instance.actual_instance = SocialAccount.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into UserIntegrationBasicAuth
        try:
            instance.actual_instance = UserIntegrationBasicAuth.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into UserIntegrationKey
        try:
            instance.actual_instance = UserIntegrationKey.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into IntegrationAccount with oneOf schemas: SocialAccount, UserIntegrationBasicAuth, UserIntegrationKey. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into IntegrationAccount with oneOf schemas: SocialAccount, UserIntegrationBasicAuth, UserIntegrationKey. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], SocialAccount, UserIntegrationBasicAuth, UserIntegrationKey]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


