/* General Styles */
QWidget {
    background-color: #212120;
    color: #FDEDCF;
    border: 1px solid #8F796A;
}

/* Buttons */
QPushButton {
    background-color: #BFDFE7;
    color: #212120;
    border: 1px solid #8F796A;
    padding: 5px;
    border-radius: 3px;
}

QPushButton:hover {
    background-color: #D4F4FE;
}

QPushButton:pressed {
    background-color: #8F796A;
}

/* LineEdit and TextEdit */
QLineEdit, QTextEdit {
    background-color: #D4F4FE;
    color: #212120;
    border: 1px solid #8F796A;
    padding: 3px;
    border-radius: 3px;
}

/* Labels */
QLabel {
    color: #FDEDCF;
}

/* GroupBox */
QGroupBox {
    background-color: #212120;
    border: 1px solid #8F796A;
    margin-top: 1ex; /* leave space at the top for the title */
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left; /* position at the top left */
    padding: 0 3px;
    background-color: #BFDFE7;
    color: #212120;
}

/* ComboBox */
QComboBox {
    background-color: #D4F4FE;
    color: #212120;
    border: 1px solid #8F796A;
    padding: 1px 0px 1px 3px; /* top, right, bottom, left */
    border-radius: 3px;
}

QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 15px;
    border-left-width: 1px;
    border-left-color: #8F796A;
    border-left-style: solid; /* just a single line */
    border-top-right-radius: 3px; /* same radius as the QComboBox */
    border-bottom-right-radius: 3px;
}

QComboBox::down-arrow {
    image: url(down_arrow.png); /* Placeholder for an arrow icon */
}

/* ScrollBar */
QScrollBar:vertical {
    border: 1px solid #8F796A;
    background: #212120;
    width: 10px;
    margin: 15px 0 15px 0;
}

QScrollBar::handle:vertical {
    background: #BFDFE7;
    min-height: 20px;
}

QScrollBar::add-line:vertical {
    border: 1px solid #8F796A;
    background: #D4F4FE;
    height: 15px;
    subcontrol-origin: bottom;
    subcontrol-position: bottom;
}

QScrollBar::sub-line:vertical {
    border: 1px solid #8F796A;
    background: #D4F4FE;
    height: 15px;
    subcontrol-origin: top;
    subcontrol-position: top;
}

QScrollBar::up-arrow:vertical, QScrollBar::down-arrow:vertical {
    background: #FDEDCF;
}

QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical {
    background: none;
}

/* CheckBox */
QCheckBox {
    color: #FDEDCF;
}

QCheckBox::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #8F796A;
    background-color: #D4F4FE;
}

QCheckBox::indicator:checked {
    background-color: #BFDFE7;
}

/* RadioButton */
QRadioButton {
    color: #FDEDCF;
}

QRadioButton::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #8F796A;
    border-radius: 6px; /* Makes it circular */
    background-color: #D4F4FE;
}

QRadioButton::indicator:checked {
    background-color: #BFDFE7;
}

/* TabWidget */
QTabWidget::pane {
    border: 1px solid #8F796A;
    background-color: #212120;
}

QTabBar::tab {
    background: #D4F4FE;
    border: 1px solid #8F796A;
    border-bottom-color: #8F796A; /* same as pane color */
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    min-width: 8ex;
    padding: 2px;
    color: #212120;
}

QTabBar::tab:selected, QTabBar::tab:hover {
    background: #BFDFE7;
}

QTabBar::tab:selected {
    border-color: #8F796A;
    border-bottom-color: #BFDFE7; /* make the selected tab appear above the pane */
}

/* MenuBar */
QMenuBar {
    background-color: #D4F4FE;
    color: #212120;
}

QMenuBar::item {
    spacing: 3px;
    padding: 1px 4px;
    background: transparent;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background: #BFDFE7;
}

QMenuBar::item:pressed {
    background: #8F796A;
}

/* Menu */
QMenu {
    background-color: #D4F4FE;
    border: 1px solid #8F796A;
    border-radius: 3px;
}

QMenu::item {
    padding: 2px 25px 2px 20px;
    border: 1px solid transparent; /* reserve space for selection border */
}

QMenu::item:selected {
    background-color: #BFDFE7;
    color: #FDEDCF;
}

QMenu::separator {
    height: 1px;
    background: #8F796A;
    margin-left: 10px;
    margin-right: 10px;
}