/* General Styles */
QWidget {
    background-color: #23202E;
    color: #EEFAFC;
    border: 1px solid #9395AB;
}

/* Buttons */
QPushButton {
    background-color: #ECB444;
    color: #23202E;
    border: 1px solid #9395AB;
    padding: 5px;
    border-radius: 3px;
}

QPushButton:hover {
    background-color: #FBF5A5;
}

QPushButton:pressed {
    background-color: #9395AB;
}

/* LineEdit and TextEdit */
QLineEdit, QTextEdit {
    background-color: #FBF5A5;
    color: #23202E;
    border: 1px solid #9395AB;
    padding: 3px;
    border-radius: 3px;
}

/* Labels */
QLabel {
    color: #EEFAFC;
}

/* GroupBox */
QGroupBox {
    background-color: #23202E;
    border: 1px solid #9395AB;
    margin-top: 1ex; /* leave space at the top for the title */
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left; /* position at the top left */
    padding: 0 3px;
    background-color: #ECB444;
    color: #23202E;
}

/* ComboBox */
QComboBox {
    background-color: #FBF5A5;
    color: #23202E;
    border: 1px solid #9395AB;
    padding: 1px 0px 1px 3px; /* top, right, bottom, left */
    border-radius: 3px;
}

QComboBox::drop-down {
    subcontrol-origin: padding;
    subcontrol-position: top right;
    width: 15px;
    border-left-width: 1px;
    border-left-color: #9395AB;
    border-left-style: solid; /* just a single line */
    border-top-right-radius: 3px; /* same radius as the QComboBox */
    border-bottom-right-radius: 3px;
}

QComboBox::down-arrow {
    image: url(down_arrow.png); /* Placeholder for an arrow icon */
}

/* ScrollBar */
QScrollBar:vertical {
    border: 1px solid #9395AB;
    background: #23202E;
    width: 10px;
    margin: 15px 0 15px 0;
}

QScrollBar::handle:vertical {
    background: #ECB444;
    min-height: 20px;
}

QScrollBar::add-line:vertical {
    border: 1px solid #9395AB;
    background: #FBF5A5;
    height: 15px;
    subcontrol-origin: bottom;
    subcontrol-position: bottom;
}

QScrollBar::sub-line:vertical {
    border: 1px solid #9395AB;
    background: #FBF5A5;
    height: 15px;
    subcontrol-origin: top;
    subcontrol-position: top;
}

QScrollBar::up-arrow:vertical, QScrollBar::down-arrow:vertical {
    background: #EEFAFC;
}

QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical {
    background: none;
}

/* CheckBox */
QCheckBox {
    color: #EEFAFC;
}

QCheckBox::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #9395AB;
    background-color: #FBF5A5;
}

QCheckBox::indicator:checked {
    background-color: #ECB444;
}

/* RadioButton */
QRadioButton {
    color: #EEFAFC;
}

QRadioButton::indicator {
    width: 13px;
    height: 13px;
    border: 1px solid #9395AB;
    border-radius: 6px; /* Makes it circular */
    background-color: #FBF5A5;
}

QRadioButton::indicator:checked {
    background-color: #ECB444;
}

/* TabWidget */
QTabWidget::pane {
    border: 1px solid #9395AB;
    background-color: #23202E;
}

QTabBar::tab {
    background: #FBF5A5;
    border: 1px solid #9395AB;
    border-bottom-color: #9395AB; /* same as pane color */
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    min-width: 8ex;
    padding: 2px;
    color: #23202E;
}

QTabBar::tab:selected, QTabBar::tab:hover {
    background: #ECB444;
}

QTabBar::tab:selected {
    border-color: #9395AB;
    border-bottom-color: #ECB444; /* make the selected tab appear above the pane */
}

/* MenuBar */
QMenuBar {
    background-color: #FBF5A5;
    color: #23202E;
}

QMenuBar::item {
    spacing: 3px;
    padding: 1px 4px;
    background: transparent;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background: #ECB444;
}

QMenuBar::item:pressed {
    background: #9395AB;
}

/* Menu */
QMenu {
    background-color: #FBF5A5;
    border: 1px solid #9395AB;
    border-radius: 3px;
}

QMenu::item {
    padding: 2px 25px 2px 20px;
    border: 1px solid transparent; /* reserve space for selection border */
}

QMenu::item:selected {
    background-color: #ECB444;
    color: #EEFAFC;
}

QMenu::separator {
    height: 1px;
    background: #9395AB;
    margin-left: 10px;
    margin-right: 10px;
}