[build-system]
requires = ["setuptools>=45", "wheel", "cython>=0.29", "numpy>=1.20"]
build-backend = "setuptools.build_meta"

[project]
name = "adaptive-formula"
version = "0.1.0"
description = "Cognitive Programming SDK"
requires-python = ">=3.8"