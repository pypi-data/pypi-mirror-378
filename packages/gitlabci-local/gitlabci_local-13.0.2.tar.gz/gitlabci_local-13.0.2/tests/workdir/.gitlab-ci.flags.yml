image: registry.gitlab.com/radiandevcore/tools/gcil/alpine:3

stages:
  - workdir

variables:
  CI_LOCAL_WORKDIR: '/tmp'

'Job 1':
  stage: workdir
  script:
    - pwd
    - ls -la ./
    - pwd | grep '^/tmp$' || exit 1
