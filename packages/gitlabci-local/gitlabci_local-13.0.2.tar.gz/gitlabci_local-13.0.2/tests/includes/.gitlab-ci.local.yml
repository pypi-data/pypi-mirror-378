include:
  - local: '/check_local.yml'

stages:
  - includes

variables:
  CI_LOCAL_NO_VERBOSE: 'true'

.local:
  variables:
    VARIABLE_2: 'OVERRIDE_2'
    VARIABLE_3: 'VARIABLE_3'
    VARIABLE_4: 4
  volumes:
    - .:/opt/src
    - ../:/opt/parent:ro
