image: registry.gitlab.com/radiandevcore/tools/gcil/alpine:3

stages:
  - home_relative

variables:
  CI_LOCAL_WORKDIR: '/mnt'

.local:
  volumes:
    - .:/mnt

'Job 1':
  stage: home_relative
  script:
    - pwd
    - ls -la ./
    - test "${PWD}" = '/mnt'
