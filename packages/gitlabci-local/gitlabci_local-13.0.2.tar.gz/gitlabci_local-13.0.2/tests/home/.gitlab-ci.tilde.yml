image: registry.gitlab.com/radiandevcore/tools/gcil/alpine:3

stages:
  - home_tilde

variables:
  CI_LOCAL_WORKDIR: '~'

.local:
  volumes:
    - ~:/mnt

'Job 1':
  stage: home_tilde
  script:
    - pwd
    - ls -la /mnt/
    - ls -la ./
    - echo "${PWD}" | grep '^/home/\|^/root'
