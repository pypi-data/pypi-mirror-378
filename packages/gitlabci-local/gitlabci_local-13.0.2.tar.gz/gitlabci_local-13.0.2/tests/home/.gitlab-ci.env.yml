image: registry.gitlab.com/radiandevcore/tools/gcil/alpine:3

stages:
  - home_env

variables:
  CI_LOCAL_WORKDIR: '/mnt'

.local:
  volumes:
    - ${HOME}:/mnt

'Job 1':
  stage: home_env
  script:
    - pwd
    - ls -la ./
    - test "${PWD}" = '/mnt'
