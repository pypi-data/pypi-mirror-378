image: registry.gitlab.com/radiandevcore/tools/gcil/alpine:3

stages:
  - volumes

variables:
  CI_LOCAL_WORKDIR: '${PWD}'

.local:
  volumes:
    - ${PWD}:${PWD}
    - ${PWD}/..:${PWD}/..

'Job 1':
  stage: volumes
  script:
    - pwd
    - ls -la ./
