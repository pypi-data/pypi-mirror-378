image: registry.gitlab.com/radiandevcore/tools/gcil/alpine:3

stages:
  - volumes

variables:
  CI_LOCAL_WORKDIR: '.'

.local:
  volumes:
    - .
    - ..

'Job 1':
  stage: volumes
  script:
    - pwd
    - ls -la ./
