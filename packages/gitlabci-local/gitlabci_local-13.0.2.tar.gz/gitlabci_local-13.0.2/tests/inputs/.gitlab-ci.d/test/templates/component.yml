spec:
  inputs:
    stage:
      default: test
      description: 'Define the pipeline stage'
    name:
      default: component
      description: 'Define the pipeline job name'
---

$[[ inputs.name ]]:
  stage: $[[ inputs.stage ]]
  script:
    - echo ' [Job] Component'
