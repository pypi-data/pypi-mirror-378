image: registry.gitlab.com/radiandevcore/tools/gcil/alpine:3

stages:
  - local

'Job 1':
  stage: local
  before_script:
    - echo '__BEFORE__'
  script:
    - set -x
    - pwd
    - ls -la ./
    - ls -la ../
  after_script:
    - echo '__AFTER__'
