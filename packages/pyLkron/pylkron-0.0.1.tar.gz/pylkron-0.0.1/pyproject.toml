[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "pyLkron"
description = "Python package to interact with Elkron Alarm devices"
dynamic = ["version"]
dependencies = [
    "requests",
]
authors = [
  {name = "Jacopo Libè", email = "jacopo.libe@gmail.com"},
]
readme = "README.md"

[build.targets.sdist]
include = ["hatch.toml"]