[project]
name = "paxel-sdk"
version = "0.1.2"
description = "Placeholder for an internal library to preserve the location to eventually be opensourced."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
