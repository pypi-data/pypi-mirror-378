[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mlhelper-stark"
version = "0.1.0"
description = "A simple machine learning helper module"
authors = [{name="Sumanbabu S", email="sumanbabu368@gmail.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "scikit-learn",
    "joblib"
]