[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "liteevallogger"
version = "0.1.0"
description = "Lightweight evaluation logger built on top of Weave"
authors = [{name="Your Name", email="you@example.com"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["weave"]
