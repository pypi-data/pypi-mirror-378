[project]
name = "retryxlib"
version = "0.1.3"          # must increment version
description = "A simple retry/backoff handler"
readme = "README.md"
requires-python = ">=3.7"
authors = [{ name = "Mohammad Razi", email = "mohdrazi1456@gmail.com" }]

[build-system]
requires = ["setuptools>=61.0", "wheel", "build"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["retryxlib"]
