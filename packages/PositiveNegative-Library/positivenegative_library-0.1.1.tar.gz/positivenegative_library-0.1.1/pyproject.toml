[build-system]
requires = ["setuptools>=61.0", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "PositiveNegative-Library"
version = "0.1.1"
description = "Check if a number is positive, negative, or zero"
readme = "README.md"
requires-python = ">=3.7"
authors = [{ name = "Rahaf Salh Qadah", email = "Rahafsalhqadah2@gmail.com" }]
license = { file = "LICENSE" }
dependencies = []

[project.scripts]
PositiveNegative-Library = "positivenegative.__main__:main"
