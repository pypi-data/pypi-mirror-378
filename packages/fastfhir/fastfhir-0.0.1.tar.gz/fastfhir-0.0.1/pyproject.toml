[project]
name = "fastfhir"
version = "0.0.1"
description = "FastFHIR: A FastAPI + FHIR toolkit (work in progress)"
authors = [
  { name="Pravin", email="pravinmd94@gmail.com" }
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
