[project]
name = "py-the-news-api"
version = "0.1.0"
description = "A Python wrapper for The News API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "pydantic",
]

[dependency-groups]
dev = [
    "dotenv>=0.9.9",
]
