[project]
name = "ai-common-utilss"
version = "0.1.3"
description = ""
authors = [
    {name = "venkatkedar007",email = "abc@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "numpy",
    "ai-text-utils"
]

[tool.poetry]
packages = [{include = "ai_common_utilss", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
train = "ai_common_utils.cli.main:main"
