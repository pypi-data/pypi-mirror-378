[build-system]
requires = ["setuptools>=61.0", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "PosNegLib"
version = "0.1.0"
description = "Check if a number is positive, negative, or zero"
readme = "README.md"
requires-python = ">=3.13"
authors = [{ name = "Ali Alsheikh", email = "a77561289@gmail.com" }]
license = { file = "LICENSE" }
dependencies = []

[project.scripts]
PosNegLib = "posneg.__main__:main"

