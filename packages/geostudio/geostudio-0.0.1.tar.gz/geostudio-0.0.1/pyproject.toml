[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "geostudio"
version = "0.0.1"
description = "Reserved package name placeholder."
authors = [
  { name="Brian Ogolla", email="Brian.Ogolla@ibm.com" }
]
requires-python = ">=3.11"
