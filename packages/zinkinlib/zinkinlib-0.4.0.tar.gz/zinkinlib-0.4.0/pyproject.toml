[project]
name = "zinkinlib"
version = "0.4.0"
description = ""
authors = [
    {name = "greenhazz",email = "timofey@gulenok.ru"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]

[tool.poetry]
packages = [{include = "zinkinlib", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
