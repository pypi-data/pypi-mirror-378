[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ediko"
version = "0.0.3"
description = "package ediko nian"
authors = [{name="Noro18"}]
license = {text = "MIT"}
