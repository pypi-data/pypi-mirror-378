; Boolean flags for turning features on/off
[flags]
; Whether or not -doc rpm is built
doc = false

; Specific bits related to *-doc rpms.
[doc]
title = Project Title
project = file-system-slug
; Multi-line data for adding additional Requires: lines data specific to the
; *-doc RPM itself
requires =

; Extra RPM related stuff for dependencies and the like
[rpm]
; Multi-line data for adding additional Requires: lines data
project = hoplite-python-nrdpd-daemon
requires =
	Obsoletes: hl-python-nrdpd-daemon
	Provides: hl-python-nrdpd-daemon = 4.0.0
