[project]
name = "agilequant-my-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "agilequant", email = "quantinvestment@163.com" }
]
requires-python = ">=3.10"
dependencies = [
    "ruff>=0.13.1",
]

[project.scripts]
my-project = "my_project:main"

[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"



[tool.hatch.build.targets.wheel]
packages = ["src/agilequant_my_project"]