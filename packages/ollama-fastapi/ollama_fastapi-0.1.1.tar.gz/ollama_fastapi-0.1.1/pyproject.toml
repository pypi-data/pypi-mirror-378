[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "ollama-fastapi"
version = "0.1.1"
description = "FastAPI wrapper for running Ollama locally"
authors = [{name = "Amal Alexander", email = "amalalex95@gmail.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = ["fastapi", "uvicorn"]

[project.scripts]
ollama-fastapi = "ollama_fastapi.cli:main"
