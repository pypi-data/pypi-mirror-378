[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "ollama-fastapi"
version = "0.1.0"
description = "FastAPI wrapper for running Ollama locally"
authors = [{name = "Your Name", email = "you@example.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = ["fastapi", "uvicorn"]

[project.scripts]
ollama-fastapi = "ollama_fastapi.cli:main"
