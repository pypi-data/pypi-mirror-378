[project]
name = "build_diagnostics"
version = "0.0.6"
authors = [
  { name="Leetbob", email="leetbob@gmail.com" },
]
description = "A package to retrieve build files for diagnostics and log them in a server"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

