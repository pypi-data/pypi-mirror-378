[project]
name = "apsniper0673_stockutil"
version = "0.1.11"
description = "My stock utility package"
authors = [
  { name = "sniper0673" }
]
requires-python = ">=3.8"
dependencies = [
    # "pandas",
    # "numpy",
]

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]