[project]
name = "mchnpkg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "helpmehelpgh", email = "helpmehelp44@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "twine>=6.2.0",
]

[tool.maturin]
module-name = "mchnpkg._core"
python-packages = ["mchnpkg"]
python-source = "src"

[tool.uv]
cache-keys = [{ file = "pyproject.toml" }, { file = "src/**/*.rs" }, { file = "Cargo.toml" }, { file = "Cargo.lock" }]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
