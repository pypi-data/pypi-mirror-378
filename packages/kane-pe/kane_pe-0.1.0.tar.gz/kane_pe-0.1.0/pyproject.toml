[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kane_pe"
version = "0.1.0"
description = "Python library για ΚΑΝ.ΕΠΕ. (Κανονισμός Επεμβάσεων)"
authors = [
  {name="Navrozidis Vasileios", email="vnavrozidis@hotmail.com"}
]
license = "MIT"
dependencies = []
requires-python = ">=3.8"

[tool.setuptools]
packages = ["kane_pe"]
