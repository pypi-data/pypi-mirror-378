<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <record id="subscription_request_form" model="ir.ui.view">
    <field name="name">subscription.request.form</field>
    <field name="model">subscription.request</field>
    <field name="inherit_id" ref="cooperator.subscription_request_view_form"/>
    <field name="arch" type="xml">
      <field name="ordered_parts" position="attributes">
        <attribute name="attrs">{'required': [('type','!=','sponsorship')]}</attribute>
      </field>
      <field name="share_product_id" position="attributes">
        <attribute name="attrs">{'required': [('type','!=','sponsorship')]}</attribute>
      </field>
      <field name="type" position="after">
        <field name="sponsor_id" attrs="{'required': [('type','=','sponsorship')], 'invisible': [('type','!=','sponsorship')]}"/>
      </field>
    </field>
  </record>
</odoo>
