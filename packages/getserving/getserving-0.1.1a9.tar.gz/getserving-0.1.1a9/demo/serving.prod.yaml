auth:
  credential_provider: credentials:CredentialProvider

routers:
  - entrypoint: routes:app
    routes:
      - path: /users/{user_id}
        permissions:
          - permission:demo:view-dashboard
      - path: /
        permissions:
          - permission:demo:view-homepage