[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "six_totp"
version = "1.0.0"
description = "six_totp is a lightweight Python library for generating 6-digit passcodes."
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Iliyan Georgiev", email = "georgiev.iliyan09@gmail.com" }
]
requires-python = ">=3.8"
dependencies = [
    # "requests>=2.0",
]

[project.urls]
Homepage = "https://github.com/GeorgievIliyan/six_totp"