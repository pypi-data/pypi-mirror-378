<Response>
    <Record fileFormat="mp3" maxLength="{max_call_length}" recordSession="{record_session}"/>
    <Stream bidirectional="true"  streamTimeout="{max_call_length}" keepCallAlive="true" contentType="audio/x-mulaw;rate=8000">{ngrok_url}</Stream>
</Response>
