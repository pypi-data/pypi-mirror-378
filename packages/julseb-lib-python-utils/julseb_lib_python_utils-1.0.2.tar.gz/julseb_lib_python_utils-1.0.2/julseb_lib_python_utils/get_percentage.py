def get_percentage(partial: float, total: float) -> float:
    """
    Returns the percentage of partial over total.

    Args:
        partial (float): The part value.
        total (float): The total value.

    Returns:
        float: The percentage value.
    """
    return (100 * partial) / total
