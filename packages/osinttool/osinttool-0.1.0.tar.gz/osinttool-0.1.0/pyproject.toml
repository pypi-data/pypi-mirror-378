[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "osinttool"
version = "0.1.0"
authors = [
  { name="Baptiste", email="tonmail@example.com" }
]
description = "Un outil OSINT qui affiche un message"
readme = "README.md"
license = { text="MIT" }
requires-python = ">=3.8"

[project.scripts]
osinttool = "osinttool.main:run"
