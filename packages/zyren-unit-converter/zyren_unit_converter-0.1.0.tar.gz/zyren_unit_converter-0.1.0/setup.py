# إعداد مكتبة unit_converter للنشر
from setuptools import setup, find_packages

setup(
    name='zyren-unit-converter',
    version='0.1.0',
    description='مكتبة لتحويل الوحدات (طول، وزن، حجم، حرارة، زمن)',
    author='Zyren_King',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.6',
)
