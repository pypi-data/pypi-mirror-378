{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/{{organization}}/{{task-id}}/{{task-version}}/{{task-id}}.schema.json",
  "additionalProperties": false,
  "title": "{{task-name}}",
  "description": "{{task-description}}",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "uses": {
      "const": "{{task}}"
    }{{with}}
  },
  "required": [
    "uses"{{required}}
  ]
}