[build-system]
requires = ["setuptools>=78", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-noveltysegmentation"
version = "0.1.0"
description = "Python implementation of Novelty Segmentation"
authors = [
    {name = "Balthazar Bujard", email = "balthazar.bujard@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "librosa",
    "scipy",
]