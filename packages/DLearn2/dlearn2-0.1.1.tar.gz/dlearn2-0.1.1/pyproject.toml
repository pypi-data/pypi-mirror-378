[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "DLearn2"
version = "0.1.1"
description = "My simple test library for lab test"
authors = [{ name = "Samosax" }]
