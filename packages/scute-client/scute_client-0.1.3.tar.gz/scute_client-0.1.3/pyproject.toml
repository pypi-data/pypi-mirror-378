[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scute-client"
version = "0.1.3"
description = "Python client for Scute API"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
  { name = "Triangle Empire", email = "dev@triangleempire.com" }
]

dependencies = [
    "requests>=2.28.0",
]
