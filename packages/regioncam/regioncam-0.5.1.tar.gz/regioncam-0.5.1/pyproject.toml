[build-system]
build-backend = "maturin"
requires = ["maturin>=1.0,<2.0"]

[project]
name = "regioncam"
dynamic = ["version"]
dependencies = ["numpy"]

[tool]

[tool.maturin]
manifest-path = "regioncam-python/Cargo.toml"
