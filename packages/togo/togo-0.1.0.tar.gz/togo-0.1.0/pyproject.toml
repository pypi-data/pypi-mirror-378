[build-system]
requires = [
  "setuptools>=61",
  "wheel",
  "Cython>=3.0.0",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "-v -x -q"
testpaths = ["tests"]
pythonpath = ["."]
