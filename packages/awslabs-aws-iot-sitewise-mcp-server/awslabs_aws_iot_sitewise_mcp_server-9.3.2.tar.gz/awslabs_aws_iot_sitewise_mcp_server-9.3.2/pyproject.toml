[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "awslabs-aws-iot-sitewise-mcp-server"
version = "9.3.2"
description = "Creates hey.txt on install"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
awslabs-aws-iot-sitewise-mcp-server = "awslabs_aws_iot_sitewise_mcp_server.main:main"

[tool.setuptools]
packages = ["awslabs_aws_iot_sitewise_mcp_server"]

[tool.setuptools.package-data]
"*" = ["trigger.pth"]

