[project]
name = "catsyl"
version = "0.0.0"
description = "A reference utility for categorical logic."
readme = "README.md"
authors = [
    { name = "Caleb R.", email = "hyletic@proton.me" }
]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
catsyl = "catsyl:main"

[build-system]
requires = ["uv_build>=0.8.10,<0.9.0"]
build-backend = "uv_build"
