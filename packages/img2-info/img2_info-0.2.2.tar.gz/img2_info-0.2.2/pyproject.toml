
[project]
name = "img2-info"
version = "0.2.2"
	description = "Im2  Python SDK (Upgraded): Clean client with auth, error handling, supports file/url/bytes/base64, includes examples and CLI, easy installation."
authors = [{name="datgsk1", email="datgsk1@gmail.com"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests>=2.28"]

[tool.setuptools]
packages = ["img2info"]
