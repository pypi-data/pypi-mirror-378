services:
  equity-aggregator:
    build: .
    container_name: equity-aggregator
    working_dir: /app
    restart: unless-stopped
    env_file:
      - .env
    environment:
      UV_ENV_FILE: ".env"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro