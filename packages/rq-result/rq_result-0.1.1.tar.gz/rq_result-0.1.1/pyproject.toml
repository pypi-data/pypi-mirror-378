[project]
name = "rq-result"
version = "0.1.1"
description = "RQ result backend for django."
readme = "README.md"
authors = [
    { name = "pyqf" }
]
requires-python = ">=3.11"
dependencies = [
    "django>=4.1.13",
]

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
