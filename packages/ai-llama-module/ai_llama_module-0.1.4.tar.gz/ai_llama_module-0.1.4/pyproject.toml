[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai_llama_module"
version = "0.1.4"
description = "Python wrapper for Llama AI API"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    { name="Isaac", email="100033398@mvla.net" }
]
license = { text = "MIT" }
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=0.21.0"
]