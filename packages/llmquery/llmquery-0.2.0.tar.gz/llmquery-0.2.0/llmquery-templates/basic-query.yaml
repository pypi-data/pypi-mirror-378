id: basic-query

metadata:
  author: "Mazin Ahmed"
  tags:
    - user-input
    - minimal
  category: "General"
  description: "A minimal template that accepts user input and returns it as-is in JSON format."

system_prompt: >
  You are an AI assistant. Your task is to answer the user's input in JSON format without any additional context or explanation.

prompt: >
  {{ user_input }}

variables:
  user_input: "What is 2*2/1+4?"
