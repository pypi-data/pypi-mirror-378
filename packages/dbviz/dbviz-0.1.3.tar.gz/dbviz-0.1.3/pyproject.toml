[project]
name = "dbviz"
version = "0.1.3"
description = "Decision Boundary Visualization for PyTorch Models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.6",
    "numpy>=1.26",
    "torch>=2.8.0",
]

[dependency-groups]
dev = [
    "ruff>=0.13.0",
]
